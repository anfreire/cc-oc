{
  "description": "On CC session end: mark every still-running cc-oc session in this CC session as detached, then prune old logs and per-spawn run dirs.",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/oc.mjs\" gc --session-id \"${CC_SESSION_ID:-$session_id}\"",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
